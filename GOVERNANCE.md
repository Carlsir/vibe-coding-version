# Vibe Coding 项目治理规则

> 我们的目标是让**完全不用 Git 的个人 AI 编码者**也能做好版本管理。
> 治理规则服务于这个目标。

---

## 核心原则

### 1. Git 让渡
检测到 Git → 本 Skill **整体退出**，不干预。

### 2. 小白优先
- 默认保守策略：不确定变更类型 → 归为"小修"
- 标记为"可运行"的版本**永远不删**
- 高级功能（变更集、被动决策）**自动运行**，不需要用户知道

### 3. 口径优先
- CHANGELOG.md 记"做了什么"（客观事实）
- PROJECT_STATE.md 记"现在是什么情况"（主观理解）
- 两个文件**同时更新**

### 4. 会话粒度
AI 的工作单位是"一次会话"，不是"一次文件修改"。
一次会话中的所有修改归为一个**变更集**，共享一个版本号。

---

## Skill 版本管理

每个项目写入 `.skill-version` 文件：

```ini
# .skill-version
skill_version=1.0
changelog_format=table_v1
naming_convention=date_type_desc_vN
```

Skill 升级时检查版本，做兼容性处理。

---

## 版本保留规则

| 规则 | 说明 |
|------|------|
| 保留最近 5 个版本 | versions/ 目录 |
| stable/ 永久保留 | 已验证可运行的版本 |
| 功能新增/重构不删 | 超出 5 个也保留 |
| 小修优先清理 | 超出 5 个时优先删 |

---

## 决策记录规范

被动决策记录触发条件：
- 确定了技术方案
- 改变了需求范围
- 决定了架构设计
- 放弃了某个方案

记录格式：
```
### [{时间}] {标题}
- 决策：{内容}
- 原因：{原因}
- 影响范围：{范围}
- 状态：已确认/待实施/已废弃
```

---

## 忽略模式规范

默认忽略：
```
temp/*
*.log
*.tmp
node_modules/
.env
__pycache__/
*.pyc
dist/
build/
```

用户可在 `.ai-version` 中自定义。

---

## 协作规范

- Fork → Branch → PR → Review → Merge
- PR 描述需说明影响范围和测试情况
- 核心规则变更需 Issue 讨论

---

## 行为准则

- 尊重不同技术选择
- 对新手友好
- 保持简洁